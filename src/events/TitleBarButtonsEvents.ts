import { createApp, ref } from "vue";

const titleBarButtonsEvents = createApp({});

export const togglePanelEvent = ref(false);
export const toastMessage = ref("");
export const toastSeverity = ref("");

export default titleBarButtonsEvents;
